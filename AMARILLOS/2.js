let NVM = "platino";

if (NVM === "platino") {

    console.log(`Usted tiene acceso a todas las areas VIP del club, gracias a su membresia ${NVM}.`);

} else if (NVM === "oro") {

    console.log(`Usted tiene acceso a las zonas VIP 1 y 2 del club.`);

} else if (NVM === "plata") {

    console.log(`Usted tiene accceso a la zona VIP 1 del club.`);

} else {

    console.log(`Usted no tiene acceso a las zonas VIP del club.`);

}
let E = "verano"; // Variable que indica la estación del año
let A = "ir a la playa"; // Actividad asociada a la estación

// Comenzamos el bucle do while
do {
    // Verificamos si la estación es "verano"
    if (E === "verano") {
        console.log(`La actividad para esta estación es: ${A}`);
    }
    // La condición del bucle es false para asegurarnos de que el bucle se ejecute solo una vez
} while (false);

let V = 90;

if (V > 80) {
    console.log(`Usted esta excediendo el limite de velocidad, su velocidad es: ${V} km/h`);
} else {
    console.log(`Usted esta dentro del limite de velocidad, su velocidad es: ${V} km/h`);
}